pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "YourWeatherApp"
include ':app'


dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "WeatherApp"
include ':app'
